return {
	projects = {
		cmake = { "CMakeLists.txt" },
		docker = { "Dockerfile", "compose.y*ml", "docker-compose.y*ml" },
		make = { "Makefile" },
		meson = { "meson.build" },
		got_git = { ".git/*" },
	},
}
